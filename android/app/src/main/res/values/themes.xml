<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real theming; this only needs to be a no-action-bar
         platform theme so the Activity window looks right before first frame. -->
    <style name="Theme.Miya" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
